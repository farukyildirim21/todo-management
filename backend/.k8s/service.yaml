apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    app: ${BACKEND_NAME}
  ports:
    - name: http
      port: 80
      targetPort: 8080
