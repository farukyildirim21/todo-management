apiVersion: v1
kind: Service
metadata:
  name: ${BACKEND_NAME}
  namespace: ${NAMESPACE}
spec:
  type: NodePort
  selector:
    app: ${BACKEND_NAME}
  ports:
    - name: http-api
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 32370