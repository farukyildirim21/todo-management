apiVersion: v1
kind: Service
metadata:
  name: ${GATEWAY_NAME}
  namespace: ${NAMESPACE}
spec:
  type: NodePort
  selector:
    app: ${GATEWAY_NAME}
  ports:
    - name: http-gateway
      protocol: TCP
      port: 80
      targetPort: 8081
      nodePort: 32380