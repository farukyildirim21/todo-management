image: docker:26

services:
  - docker:26-dind

variables:
  DOCKER_TLS_CERTDIR: ""
  DOCKER_DRIVER: overlay2

stages:
  - build

ci-build:
  stage: build
  script:
    - docker version
    - docker compose build
